<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>学生主界面</title>
    <link rel="stylesheet" type="text/css" href="public/bootstrap.min.css">
    <style>
        body {
            display: flex;
            font-family: Arial, sans-serif;
        }
        .sidebar {
            width: 200px;
            background-color: #f4f4f4;
            padding: 15px;
        }
        .content {
            flex-grow: 1;
            padding: 20px;
        }
        .menu-item {
            margin: 10px 0;
        }
        .hidden {
            display: none;
        }
    </style>
</head>
<body>
<div class="sidebar">
    <h2>菜单</h2>
    <div class="menu-item"><a href="/student_main">主页</a></div>
    <div class="menu-item"><a href="#" id="enroll-link">选课</a></div>
</div>
<div class="content">
    <h1>欢迎来到学生主界面</h1>
    <div id="enroll-section" class="hidden">
        <h2>可选课程</h2>
        <p>课程数量: <%= courses.length %></p>
        <form action="/enroll" method="POST">
            <div class="menu-item">
                <table>
                    <thead>
                    <tr>
                        <th>课程名称</th>
                        <th>学分</th>
                        <th>学期</th>
                        <th>教师ID</th>
                        <th>学院ID</th>
                        <th>上课时间</th>
                        <th>选择</th>
                    </tr>
                    </thead>
                    <tbody>
                    <% if (courses.length > 0) { %>
                        <% courses.forEach(function (item) { %>
                            <tr>
                                <td><%= item.course_name %></td>
                                <td><%= item.credits %></td>
                                <td><%= item.semester %></td>
                                <td><%= item.teacher_id_c %></td>
                                <td><%= item.department_id_c %></td>
                                <td><%= item.class_time %></td>
                                <td><input type="checkbox" name="courseId" value="<%= item.ID %>"></td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="7">没有可选课程。</td>
                        </tr>
                    <% } %>
                    </tbody>
                </table>
            </div>
            <button type="submit">选课</button>
        </form>
    </div>
</div>
<script>
    document.getElementById('enroll-link').onclick = function() {
        const enrollSection = document.getElementById('enroll-section');
        enrollSection.classList.toggle('hidden');
    };
</script>
</body>
</html>